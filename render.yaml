services:
  - type: web
    name: football-api
    runtime: node
    buildCommand: npm install && npx prisma generate --schema=src/prisma/schema.prisma
    startCommand: npm start
    envVars:
      - key: DATABASE_URL
        sync: false
      - key: JWT_SECRET
        sync: false
      - key: NODE_ENV
        value: production